---
import type { PortfolioProject } from "../data/projects";

type Props = {
  project: PortfolioProject;
  label?: string;
};

const { project, label = "Case Study" } = Astro.props;
const hasCover = Boolean(project.cover);
---

<a class="project-card" href={`/projects/${project.slug}`}>
  <article class="card">
    {hasCover ? (
      <img class="cover" src={project.cover} alt={`${project.title} cover image`} loading="lazy" />
    ) : (
      <div class="cover-placeholder">
        <span>{project.tags[0]}</span>
      </div>
    )}

    <div class="card-top">
      <p class="project-label">{label}</p>
      <span aria-hidden="true">→</span>
    </div>

    <h4>{project.title}</h4>
    <p class="subtitle">{project.subtitle}</p>

    {project.headlineMetric && (
      <p class="metric">{project.headlineMetric}</p>
    )}

    <div class="tags">
      {project.tags.slice(0, 5).map((tag) => <span>{tag}</span>)}
    </div>
  </article>
</a>

<style>
  .project-card {
    text-decoration: none;
    display: block;
  }

  .project-card .card {
    height: 100%;
    overflow: hidden;
    border-radius: 8px;
    transition:
      transform 160ms ease,
      border-color 160ms ease;
  }

  .project-card:hover .card {
    transform: translateY(-4px);
    border-color: rgba(103, 232, 249, 0.55);
  }

  .cover,
  .cover-placeholder {
    width: calc(100% + 48px);
    margin: -24px -24px 22px;
    height: 160px;
    border-bottom: 1px solid rgba(148, 163, 184, 0.22);
  }

  .cover {
    object-fit: cover;
    display: block;
    background: rgba(2, 6, 23, 0.55);
  }

  .cover-placeholder {
    display: grid;
    place-items: center;
    background:
      linear-gradient(135deg, rgba(59, 130, 246, 0.12), rgba(20, 184, 166, 0.08)),
      rgba(2, 6, 23, 0.55);
  }

  .cover-placeholder span {
    color: #67e8f9;
    font-weight: 800;
    letter-spacing: 0;
    text-transform: uppercase;
    font-size: 12px;
    text-align: center;
    padding: 0 20px;
  }

  .card-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
    color: #67e8f9;
    font-weight: 800;
    margin-bottom: 16px;
  }

  .project-label {
    margin: 0;
    color: #67e8f9;
    font-size: 13px;
    letter-spacing: 0;
    text-transform: uppercase;
  }

  h4 {
    margin: 0;
    font-size: 20px;
    color: #e5eef7;
  }

  .subtitle {
    color: #cbd5e1;
    line-height: 1.65;
  }

  .metric {
    margin: 18px 0;
    padding-top: 14px;
    border-top: 1px solid rgba(148, 163, 184, 0.2);
    color: #67e8f9;
    font-size: 14px;
    font-weight: 800;
  }
</style>
